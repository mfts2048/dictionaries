// ----------------------- slice(0) --------------------------------
// const obj = { name: 1 };
// const arr = new Array(2).fill(obj);
// const newArr = arr.slice(0);
// arr[0].name = 2;

// console.log(arr);
// console.log(newArr);

// ----------------------- Object.assign ----------------------------
// const newObj = Object.assign({}, oldObj);

// ----------------------- ...(拓展运算符) -----------------------------
// const newObj = { ...oldObj };
